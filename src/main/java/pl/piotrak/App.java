package pl.piotrak;

/**
 * Some decisions were made in regard to the system functionalities:
 * I tried to produce a working application, so Hibernate was used, it only needs a valid database and some configuration in hibernate.cfg.xml
 * - h2database could have been used for in-memory database
 * System.out was used for message printing
 */
public class App {

    @SuppressWarnings("resource")
    public static void main(String[] args) {
    }
}
